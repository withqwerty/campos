{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://campos.football/schema/shot-event.schema.json",
  "title": "ShotEvent",
  "description": "Canonical shot event normalized into Campos pitch space.",
  "type": "object",
  "required": [
    "kind",
    "id",
    "matchId",
    "teamId",
    "playerId",
    "playerName",
    "minute",
    "addedMinute",
    "second",
    "period",
    "x",
    "y",
    "xg",
    "outcome",
    "bodyPart",
    "isOwnGoal",
    "isPenalty",
    "context",
    "provider",
    "providerEventId"
  ],
  "additionalProperties": false,
  "dependentRequired": {
    "endX": ["endY"],
    "endY": ["endX"]
  },
  "properties": {
    "kind": {
      "const": "shot",
      "description": "Discriminant for the event union."
    },
    "id": {
      "type": "string",
      "description": "Stable Campos event identifier."
    },
    "matchId": {
      "type": "string",
      "description": "Match identifier in the consuming app or source dataset."
    },
    "teamId": {
      "type": "string",
      "description": "Identifier for the team taking the shot."
    },
    "playerId": {
      "type": ["string", "null"],
      "description": "Provider player identifier when available."
    },
    "playerName": {
      "type": ["string", "null"],
      "description": "Display-ready player name when present in the source feed."
    },
    "minute": {
      "type": "integer",
      "minimum": 0,
      "maximum": 200
    },
    "addedMinute": {
      "type": ["integer", "null"],
      "minimum": 0,
      "description": "Stoppage-time minute offset, null when not in added time."
    },
    "second": {
      "type": "integer",
      "minimum": 0,
      "maximum": 59
    },
    "period": {
      "type": "integer",
      "enum": [1, 2, 3, 4, 5],
      "description": "Match period: 1-2 regular halves, 3-4 extra time, 5 penalty shootout."
    },
    "x": {
      "type": ["number", "null"],
      "minimum": 0,
      "maximum": 100,
      "description": "Campos pitch x-coordinate (0 = own goal line, 100 = opposition goal line). Null for events without coordinates."
    },
    "y": {
      "type": ["number", "null"],
      "minimum": 0,
      "maximum": 100,
      "description": "Campos pitch y-coordinate (0 = attacker's right, 100 = attacker's left). Null for events without coordinates."
    },
    "endX": {
      "type": ["number", "null"],
      "minimum": 0,
      "maximum": 100,
      "description": "Campos pitch x-coordinate where the shot ended (e.g. freeze-frame or carry-through location toward goal or wide). Null when unavailable. Must be present with endY when set."
    },
    "endY": {
      "type": ["number", "null"],
      "minimum": 0,
      "maximum": 100,
      "description": "Campos pitch y-coordinate where the shot ended. Null when unavailable. Must be present with endX when set."
    },
    "xg": {
      "type": ["number", "null"],
      "minimum": 0,
      "description": "Expected goals value from the provider model."
    },
    "xgot": {
      "type": ["number", "null"],
      "minimum": 0,
      "description": "Post-shot expected goals (xG on target). Null when unavailable."
    },
    "outcome": {
      "type": "string",
      "enum": ["goal", "saved", "blocked", "off-target", "hit-woodwork", "other"]
    },
    "bodyPart": {
      "type": ["string", "null"],
      "enum": ["left-foot", "right-foot", "head", "other", null]
    },
    "isOwnGoal": {
      "type": "boolean"
    },
    "isPenalty": {
      "type": "boolean"
    },
    "context": {
      "type": ["string", "null"],
      "enum": [
        "regular-play",
        "fast-break",
        "set-piece",
        "from-corner",
        "direct-free-kick",
        "penalty",
        "other",
        null
      ]
    },
    "provider": {
      "type": "string",
      "description": "Source provider identifier such as opta, statsbomb, or whoscored."
    },
    "providerEventId": {
      "type": "string",
      "description": "Raw event identifier from the source provider."
    },
    "goalMouthY": {
      "type": ["number", "null"],
      "minimum": 0,
      "maximum": 100,
      "description": "Where the shot crossed the goal plane on the horizontal axis (0 = left post, 100 = right post). Null when not available or shot missed wide."
    },
    "goalMouthZ": {
      "type": ["number", "null"],
      "minimum": 0,
      "maximum": 100,
      "description": "Where the shot crossed the goal plane on the vertical axis (0 = ground, 100 = crossbar height). Null when not available or shot missed high/wide."
    },
    "sourceMeta": {
      "type": ["object", "null"],
      "additionalProperties": true,
      "description": "Provider-scoped metadata bag for provenance and advanced debugging. It must stay JSON-serializable, must not duplicate canonical top-level fields, and must not reinterpret raw provider semantics into fake cross-provider meaning."
    }
  }
}
