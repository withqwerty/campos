# v0.2 Overnight Work Log

**Status:** archived scratch log
**Note:** Historical execution scratch pad. Not an active source of truth for roadmap or ship criteria.

## 2026-04-09

- **22:00** — Launched 3 parallel agents: Packet 0 (ShotMap cleanup), Packet 1 (PassMap spec), Packet 2 (ScatterPlot spec)
- **22:01** — Diagnostics show ShotMap cleanup agent has type errors — constants removed but still referenced. Monitoring agent to see if it self-corrects.
- **22:02** — All 3 agents confirmed actively working. ShotMap agent mid-refactor (type errors expected during edit). PassMap agent reading ref code INDEX + adapter map-pass files. ScatterPlot agent reading template + schema.
- **22:02** — User going to bed. Autonomous mode: deliver as many components as possible into reviewable state. Run review loops, iterate, visually review. Keep parallelizing.
- **22:03** — Launched background mplsoccer reference pre-read (passes, heatmaps, scatter, arrow primitives) to feed implementation packets.
- **22:03** — Decision: Packet 5 (Heatmap spec) has no dependencies — will launch it as soon as a slot frees up to keep pipeline full.
- **22:03** — Set up background keepalive checks on agent output files.
- **22:07** — **Packet 0 COMPLETE.** ShotMap cleanup PR: https://github.com/withqwerty/campos/pull/1. Removed 10 hardcoded constants, 2 transform functions, ~60 lines SVG markup → replaced with stadia `<Pitch>`. Net -43 LOC. 209 tests pass. 5 shared seams marked with TODO(shared-seam).
- **22:07** — Decision: Launch Packet 5 (Heatmap spec) now — it has no dependencies. Also, Packets 1+2 (spec agents) still running.
- **22:07** — Launched Packet 5 (Heatmap spec) agent in worktree.
- **22:07** — mplsoccer pre-read complete. Key findings: arrows use quiver, heatmaps use bin_statistic+pcolormesh, lines use LineCollection with comet/transparent effects. Saved for implementation packets.
- **22:10** — **Packet 1 COMPLETE.** PassMap spec written but agent didn't commit. Manually committed, formatted, pushed, PR created: https://github.com/withqwerty/campos/pull/2
- **22:10** — Decision: Run L1 adversarial review on PassMap spec. Also, Packet 1 landing unblocks Packet 3 (PassMap impl). Will launch Packet 3 after L1 review.
- **22:10** — Launched L1 adversarial review on PassMap spec + Packet 3 (PassMap impl) agent in parallel.
- **22:16** — **Packet 2 COMPLETE.** ScatterPlot spec PR: https://github.com/withqwerty/campos/pull/3. Generic `ScatterPlot<T>` API, Cartesian axes as new internal primitive, 15 edge cases documented. No football-viz reference covers Cartesian scatter — new ground.
- **22:16** — Decision: Launch L1 adversarial review on ScatterPlot spec + Packet 4 (ScatterPlot impl) in parallel.
- **22:16** — Launched L1 ScatterPlot review + Packet 4 (ScatterPlot impl) agents.
- **22:20** — **L1 PassMap review COMPLETE.** 9 findings. 2 high-severity blockers: (1) StatsBomb pass.height null-safety crash, (2) Line layer primitive has no spec/type contract. 7 medium/low findings including Opta recipient underreported, Wyscout playerName always null, dense tooltip z-order, adapter gap matrix inconsistency.
- **22:20** — Decision: Fix the two blockers now. StatsBomb null guard is a code fix. Line layer — scope as PassMap-internal for v0.2, defer reuse contract to v0.3 (option b from review). Also fix the medium findings in the spec.
- **22:25** — Fixed both high-severity blockers + all medium findings: StatsBomb pass.height null guard committed on main (8403fa0). Wyscout through-ball mapping (subEventId 85) added. PassMap spec updated with all 9 L1 review findings. Spec fixes pushed to PR #2.
- **22:25** — PassMap impl agent still running (seeing type diagnostics — active coding). ScatterPlot impl agent also running (generic type issues, expected). Heatmap spec + ScatterPlot L1 review also in flight.
- **22:30** — **Packet 5 COMPLETE.** Heatmap spec PR: https://github.com/withqwerty/campos/pull/4. Uniform grid only, hexbin/KDE/Voronoi deferred to v0.3.
- **22:30** — **ScatterPlot L1 review COMPLETE.** 9 findings: (1) generic typing runtime unsafety on xKey/yKey, (2) categorical/continuous 8-value cliff, (3) legend/scaleBar naming divergence from ShotMap, (4) nice-ticks underspecified for negatives/zero-crossing, (5) padding units unspecified, (6) pointIndex instability for React keys, (7) opacity 50-point cliff, (8) empty state axis contradiction, (9) headerStats undefined defaults.
- **22:30** — Decision: Launch Heatmap L1 review + Packet 6 (Heatmap impl). Apply ScatterPlot review fixes to the spec PR. PassMap + ScatterPlot impl agents still running.
- **22:35** — Heatmap spec committed and pushed, PR: https://github.com/withqwerty/campos/pull/4. Launched Heatmap L1 review + Packet 6 impl agent.
- **22:38** — ScatterPlot L1 review fixes applied and pushed to PR #3: added colorMode prop, aligned legend/scaleBar naming with ShotMap, clarified nice-ticks (D3 1-2-5), defined empty state axes, defined headerStats defaults, added grid passthrough.
- **22:38** — Diagnostics show active coding in ScatterPlot impl (JSX flag — worktree tsconfig issue) and Heatmap impl (object possibly undefined — mid-edit). Both expected.
- **22:42** — **Heatmap L1 review COMPLETE.** 10 findings: (1) coordinate space 0-100 vs 0-1 schema mismatch — HIGH, (2) input type bypasses Event union — HIGH, (3) scale bar transparent start — MEDIUM, (4) boundary event binning formula — MEDIUM, (5) ShotMap layout divergence undocumented — MEDIUM, (6) normalization float precision — LOW, (7) provider readiness contradicts gap matrix — MEDIUM, (8) custom colorScale without stops — LOW, (9) half-pitch grid distortion — LOW, (10) sparse threshold arbitrary — LOW.
- **22:42** — Decision: Apply key fixes to Heatmap spec. For Finding 1 (coordinate space), confirmed Campos runtime uses 0-100 (JSDoc is authoritative, JSON schema min/max is stale). For Finding 4, specify exact bin formula with clamping.
- **22:45** — Heatmap L1 review fixes applied and pushed to PR #4: exact bin formula, scale bar starts at non-zero color, input type assignability note, colorScale/colorStops interaction.
- **22:45** — All 3 L1 adversarial reviews complete and fixes applied to all 3 spec PRs. All specs now have Loop 1 done. Waiting on 3 implementation agents (PassMap, ScatterPlot, Heatmap).
- **22:55** — **Packet 3 COMPLETE.** PassMap implementation PR: https://github.com/withqwerty/campos/pull/5. Core compute, React renderer, 14 tests, demo page with 5 variants. Color-deduplicated arrowhead markers.
- **22:55** — **Packet 4 COMPLETE.** ScatterPlot implementation PR: https://github.com/withqwerty/campos/pull/6. Core compute with nice-ticks + linear scale, React renderer, 26 new tests (238 total), shared color.ts extracted. Fixed lint errors (void expressions) and formatting before commit.
- **22:55** — User noted: can merge PRs to main if it simplifies conflict resolution. Decision: will merge spec PRs and ShotMap cleanup to main now to reduce conflicts for Heatmap impl. Heatmap impl agent still running.
- **23:05** — Merged all 6 PRs to main: #1 (ShotMap cleanup), #2 (PassMap spec), #3 (ScatterPlot spec), #4 (Heatmap spec), #5 (PassMap impl), #6 (ScatterPlot impl). Resolved 3 sets of merge conflicts (matrix.md, adapter-gap-matrix.md, Nav.astro).
- **23:05** — Heatmap impl agent (Packet 6) still running. Only remaining v0.2 deliverable. Once it lands: all 3 components implemented with specs, L1 reviews, core, React, demos, and tests.
- **23:10** — **Packet 6 COMPLETE.** Heatmap implementation PR: https://github.com/withqwerty/campos/pull/7. Core compute with uniform grid binning, React renderer with stadia pitch overlays, 12 new tests, demo page with 4 variants.
- **23:12** — Resolved merge conflicts (color.ts, index.ts, shot-map.ts, react/index.ts, Nav.astro). Merged PR #7 to main.
- **23:15** — Fixed Wyscout test (passes[0] is subEventId 85, now correctly "through-ball"). Committed on main.
- **23:15** — Cleaned up all 7 worktrees. Full test suite: **268 tests, 26 files — ALL PASSING.**
- **23:15** — Pushed all to main. All worktrees removed.

## Final Summary

**All 7 v0.2 dispatch queue packets delivered and merged:**

| #   | Packet                         | PR  | Tests        | Status |
| --- | ------------------------------ | --- | ------------ | ------ |
| 0   | ShotMap foundation cleanup     | #1  | 209 pass     | merged |
| 1   | PassMap spec (L1 reviewed)     | #2  | n/a          | merged |
| 2   | ScatterPlot spec (L1 reviewed) | #3  | n/a          | merged |
| 3   | PassMap implementation         | #5  | 14 new tests | merged |
| 4   | ScatterPlot implementation     | #6  | 26 new tests | merged |
| 5   | Heatmap spec (L1 reviewed)     | #4  | n/a          | merged |
| 6   | Heatmap implementation         | #7  | 12 new tests | merged |

**Additional work completed:**

- 3 L1 adversarial reviews with findings applied to all specs
- StatsBomb pass.height null guard fix
- Wyscout through-ball mapping fix (subEventId 85)
- Shared color utilities extracted (core/color.ts)
- Shared scale utilities created (core/scales/)
- mplsoccer reference pre-read for implementation guidance

**Final main state:** 268 tests passing, all typechecks clean, all hooks passing.

**Remaining for v0.2 completion:** L2 implementation reviews, L3 release-readiness reviews, visual demo review, status matrix updates to reflect implementation progress.

## 2026-04-09 — Codex takeover

- **03:06** — Started the requested 30-minute monitor loop from the main checkout. Snapshot captured active overnight worktree activity, open local change in `docs/status/matrix.md`, and recent merges still landing on `main`.
- **03:36** — First wake snapshot showed the overnight packets had been merged to `main`, worktrees had been cleaned down to the main checkout, and the repo had advanced to `40d8c92`.
- **04:06** — Second wake snapshot matched the first one. No repo activity between checks, so I took over review/fix duties.
- **04:07** — Read this overnight log, inspected the merged packets and current tree, and started verification. Confirmed the expected packet set landed on `main`: ShotMap cleanup, PassMap spec + implementation, ScatterPlot spec + implementation, Heatmap spec + implementation, plus the Wyscout test update.
- **04:08** — Verification pass:
  - `pnpm test` passed on the overnight tree before any takeover edits.
  - `pnpm typecheck` passed when run on its own.
  - `pnpm --filter @withqwerty/campos-site build` passed.
  - Note: one earlier `typecheck` failure was caused by my own parallel `build` + `typecheck` run racing on `dist/` outputs, not by the overnight code.
- **04:09** — Implementation review findings from takeover:
  - No code regressions found in the merged `PassMap`, `ScatterPlot`, or `Heatmap` core models under the current test/build suite.
  - Standards gap found: no React renderer tests existed for `PassMap`, `ScatterPlot`, or `Heatmap` even though those components had shipped demos and core tests.
  - Accessibility gap found: `Heatmap` tooltip interaction was mouse-only because non-empty cells were not keyboard-focusable.
  - Tracking gap found: `docs/status/matrix.md` still reflected pre-merge packet/component status.
- **04:10** — Confident fixes applied:
  - Added React renderer tests for:
    - `packages/react/test/PassMap.test.tsx`
    - `packages/react/test/ScatterPlot.test.tsx`
    - `packages/react/test/Heatmap.test.tsx`
  - Updated `packages/react/src/Heatmap.tsx` so non-empty cells are focusable and expose tooltip content on keyboard focus as well as hover.
  - Normalized `packages/core/src/heatmap.ts` `meta.component` to `\"Heatmap\"` for consistency with the chart naming used elsewhere.
  - Updated `docs/status/matrix.md` to reflect delivered packets, demo pages, and the current review state.
- **04:11** — Post-fix verification:
  - `pnpm test` → **277 tests passing**
  - `pnpm typecheck` → passing
  - `pnpm --filter @withqwerty/campos-site build` → passing
- **04:11** — Remaining follow-up risks after takeover review:
  - `PassMap`, `ScatterPlot`, and `Heatmap` still need a fuller L3 release-readiness pass, especially manual visual review against their demo pages.
  - `ScatterPlot` marker identity remains index-based; acceptable for current demos/tests, but still worth revisiting if stable selection across data reordering becomes important.
